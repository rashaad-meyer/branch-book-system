DATABASE_URL="postgresql://postgres:postgres@localhost:5432/branch_booking"
PORT=4000
# Secret used to sign JWTs. Must be at least 16 characters. Use a strong,
# random value in production (e.g. `openssl rand -base64 32`).
JWT_SECRET="change-me-to-a-long-random-secret"
